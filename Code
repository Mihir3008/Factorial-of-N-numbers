#include <iostream> 
using namespace std;  
int main()  
{  
  int fact,num,i;
  fact = 1;
  cout << "Enter the Number: ";  
  cin >> num;  
  for(i = 1; i <= num; i++)
  {
      fact = fact * i;
  }
  cout << "The factorial of a inputted number is: " << fact;
  return 0;  
}  
